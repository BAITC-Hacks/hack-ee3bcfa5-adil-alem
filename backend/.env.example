# Optional SQLite override, relative to the directory where you start Uvicorn.
DATABASE_URL=sqlite:///./sana.db
# Interview and execution test. Scores remain calculated by the Python engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
